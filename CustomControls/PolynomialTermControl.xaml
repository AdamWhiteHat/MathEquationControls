<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
					xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
					xmlns:local="clr-namespace:MathEquationControl"
					xmlns:converters="clr-namespace:MathEquationControl.ValueConverters"
					mc:Ignorable="d">

	<Style TargetType="{x:Type local:PolynomialTermControl}">

		<Style.Resources>
			<converters:StringToBigIntegerConverter x:Key="String2BigIntegerConverter" />
		</Style.Resources>

		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type local:PolynomialTermControl}">
					<Border x:Name="PART_Border"
							Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
							Width="{TemplateBinding Width}"
							Height="{TemplateBinding Height}"
							HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
							VerticalAlignment="{TemplateBinding VerticalAlignment}" >

						<RichTextBox IsReadOnly="True" IsReadOnlyCaretVisible="False" x:Name="PART_RichTextBox" Style="{DynamicResource RichTextBoxStyle}"  AcceptsReturn="False" >

							<FlowDocument>
								<Paragraph Style="{DynamicResource FocusableParagraph}">
									<TextBlock x:Name="PART_TextBlock" Style="{DynamicResource TextBlockStyle}" ></TextBlock>
								</Paragraph>
							</FlowDocument>

						</RichTextBox>

					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>