<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:local="clr-namespace:MathEquationControl">

	<Style TargetType="{x:Type local:PolynomialTermControl}">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type local:PolynomialTermControl}">
					<Border x:Name="PART_Border"
							Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
							Width="{TemplateBinding Width}"
							Height="{TemplateBinding Height}"
							HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
							VerticalAlignment="{TemplateBinding VerticalAlignment}" >

						<DockPanel LastChildFill="True" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
						
							<RichTextBox x:Name="PART_RichTextBox"
										 Style="{DynamicResource RichTextStyle}" >
								<FlowDocument x:Name="PART_FlowDocument"  Style="{DynamicResource FlowDocumentStyle}"  >
									
									<Table>
										<TableRowGroup>
											<TableRow>
												<TableCell>
													<BlockUIContainer>
														<Grid VerticalAlignment="Center">
															<Grid.RowDefinitions>
																<RowDefinition Height="*" />
															</Grid.RowDefinitions>
															<TextBlock Grid.Row="0" Style="{DynamicResource TextBlockStyle}">															
																<Run Name="PART_Coefficient" Text="0"  /><Run Name="PART_MultiplicationSymbol" Text="*" /><Run Name="PART_Indeteminant" Text="x" /><Run Name="PART_Exponent" Text="2" Style="{DynamicResource Exponent}" /></TextBlock>
														</Grid>
													</BlockUIContainer>
												</TableCell>
											</TableRow>
										</TableRowGroup>
									</Table>

								</FlowDocument>
							</RichTextBox>
							
						</DockPanel>
						
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>